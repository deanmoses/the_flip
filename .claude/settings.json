{
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "command -v ffmpeg || (apt-get update -qq 2>/dev/null && apt-get install -y -qq libheif1 libheif-dev ffmpeg 2>/dev/null) || (sudo apt-get update -qq && sudo apt-get install -y -qq libheif1 libheif-dev ffmpeg)"
      }
    ]
  }
}
